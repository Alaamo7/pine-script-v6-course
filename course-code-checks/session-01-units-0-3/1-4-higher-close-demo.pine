//@version=6
indicator("Higher Close Demo", overlay=true)

isHigher = close > close[1]
plotshape(isHigher, title="Higher Close", style=shape.triangleup, location=location.belowbar, color=color.green)
